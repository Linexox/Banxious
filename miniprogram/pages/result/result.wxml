<view class="container night-mode">
  <!-- 始终存在的 Canvas，用于动画，层级最高但初始隐藏 -->
  <canvas type="2d" id="destroyCanvas" class="destroy-canvas" style="visibility: {{destroyed ? 'visible' : 'hidden'}}; pointer-events: none;"></canvas>

  <view wx:if="{{loading}}" class="loading-text">
    <text>正在提取焦虑样本...</text>
  </view>

  <view wx:elif="{{errorMsg}}" class="error-container">
    <text class="error-text">⚠️ {{errorMsg}}</text>
    <button class="retry-btn" bindtap="generateCard">重试</button>
  </view>

  <view wx:elif="{{cardData}}" class="card-container" bindtap="onFlipCard">
    <view class="card-flipper {{isFlipped ? 'flipped' : ''}}" style="opacity: {{destroyed ? 0 : 1}};">
      
      <!-- 正面 -->
      <view class="card card-front">
        <view class="card-header">
          <text class="tag">{{cardData.mood_tag}}</text>
        </view>
        <view class="card-body">
          <text class="encouragement">{{cardData.encouragement}}</text>
          <view class="hint">👆 点击翻转查看建议</view>
        </view>
        <view class="card-footer">
          <text class="quote">“{{cardData.healing_quote}}”</text>
        </view>
      </view>

      <!-- 反面 -->
      <view class="card card-back">
        <view class="card-header">
          <text class="tag">Mood Lab 建议</text>
        </view>
        <view class="card-body">
          <view class="suggestion-list">
            <block wx:for="{{cardData.suggestions}}" wx:key="index">
              <view class="suggestion-item">
                <text class="bullet">•</text>
                <text class="content">{{item}}</text>
              </view>
            </block>
          </view>
        </view>
        <view class="card-footer">
          <text class="quote">“{{cardData.healing_quote}}”</text>
        </view>
      </view>

    </view>
  </view>

  <view wx:if="{{!loading && !destroyed && !errorMsg}}" class="action-area">
    <view class="destroy-options">
      <view class="destroy-btn-wrapper" bindtap="onTear">
        <view class="destroy-icon-btn tear">✂️</view>
        <text class="destroy-label">撕碎</text>
      </view>
      <view class="destroy-btn-wrapper" bindtap="onBurn">
        <view class="destroy-icon-btn burn">🔥</view>
        <text class="destroy-label">烧毁</text>
      </view>
      <view class="destroy-btn-wrapper" bindtap="onCrush">
        <view class="destroy-icon-btn crush">💥</view>
        <text class="destroy-label">粉碎</text>
      </view>
    </view>
  </view>
  
  <view wx:if="{{destroyed}}" class="ashes">
    <text>✨</text>
  </view>
</view>
